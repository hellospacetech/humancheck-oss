# Changesets

Bu klasör [Changesets](https://github.com/changesets/changesets) tarafından yönetilir.

Her değişiklik için `npx changeset` komutuyla yeni bir changeset oluşturun.
Release yapmak için `npx changeset version` ve ardından `npx changeset publish` kullanın.
